version: "3.9"

services:
  backend:
    build: ./backend
    container_name: scholarmatch-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: scholarmatch-frontend
    ports:
      - "80:80"
    env_file:
      - ./frontend/.env_file
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    restart: unless-stopped
